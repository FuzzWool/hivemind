Let there be Cool Moves
===========
Nut is born, and she has basic controls.
She can walk across slopes.

Goals:
x Make Nut with basic left and right controls.
x Allow Nut to run across 1/2-block slopes.

x Add a DIVE.
x Add a WALL JUMP.
x Add CRAWLING.
x Add a SLIDE KICK.

! Add PUNCHING.




====

GOAL: Clean up ROOM and WORLDMAP code.
ROOM and WORLDMAP has become unruly.


! WIP - Ported ROOM and WORLDMAP to the main game.

	! Support for COLLISIONS.
		load and bind collision data.

		* COLLISIONS are LOADED (and have a default otherwise)
			* Static loads/creates shared collision data.
			* Rooms position shared data to match layout

		! ENTITIES may collide with WORLDMAP.
			

			* Fix LevelEditor crashes


			! Make the following classes independant,
			from MYSPRITE to POSITIONED OBJECT:
				! next
				! collision
				! slope_collision



! Clean up ROOM and WORLDMAP.
! Create a new LEVEL EDITOR.

-

! Make collision independant of a sprite.
Instead, collisions can be assigned to a rect.


====

! Holding both directional keys causes weird Nut walking.

! Fix lag issue while moving around the WorldMap.
	* Investigate the stress empty MySprites put on.

! Add a system for assigning collision data to a tilemap.
