Let there be Levels
===========
Seamless loading from level to level. No transitions.

MAJOR Goals:
* Append a level to another level from two level files.
* Gradually load and de-load levels during exploration.
* Append levels based on a world file's data.
* A mini world map may determine which 'block' the player is in

=
0.0.8 - Multiple levels
Goals:
* Ctrl+O opens a new Level. (+ Double-click empty space)

* Cursor should have coordinates binded to an ELevel.

x ELevels may be altered individually.
* A WorldMap saves the positioning of all these levels.

-

x TileSelections load different palettes from level to level.
x The palette is stored at the top of a level file.
	x Add saving.

x Properties: changing palette on the go.
Make a new Tkinter window.

Fix TileSelections being limited to a level highlight.

save WorldMap.

Main game:
Test ordinary Levels. (Implement RenderTextures)
	Cleanup and affirm differences between ELevel and Level.
Add the WorldMap loading.